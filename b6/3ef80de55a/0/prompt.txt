/feature-pipeline SMA-581

---

approved

---

<task-notification>
<task-id>a9803493f69d44480</task-id>
<tool-use-id>toolu_01SPp6R3WjTFq5zcHoH28Pc5</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-smaschek-dev-paigasus-paigasus-helikon/851df2c8-94b1-4119-8ab2-ab939461c13e/tasks/a9803493f69d44480.output</output-file>
<status>completed</status>
<summary>Agent "Adversarial spec challenge" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Verdict: NEEDS REWORK

The spec's instincts are right (denylist over allowlist, one rule disabled, a self-test against silent degradation, correcting the ticket's premise with real numbers) and its arithmetic is internally consistent. But three of its four concrete deliverables have defects that must be settled before code: the ruleset change is inert without an unmentioned manual step, the self-test is not implementable as written and would validate a different linter version than the gate, and the proposed glob set demonstrably breaks the "local run == CI run" invariant it is built around on the maintainer's own machine. One prescribed hand-fix also provably introduces new violations of a rule the same PR enables.

## Findings

### BLOCKER

- **Editing `main-protection-checks.json` does not make the check required; nothing in the spec runs the applier** - The spec's only step is "`{ "context": "markdown-lint" }` is added to `.github/rulesets/main-protection-checks.json`", and Verification #5 asks only that the context "is **listed in**" that file. But `/private/tmp/.../wt-sma-581/CONTRIBUTING.md` (Repo configuration) states the JSON is applied by `scripts/apply-repo-config.sh` and that "There is no drift-check CI job — divergence is detected by the next maintainer running the script." | Why it matters: Two failure modes, both bad. If nobody runs the script, the repo *documents* a required gate that GitHub does not enforce — a green-looking policy that enforces nothing, which is exactly the class of bug the spec's own self-test section exists to prevent. If the script is run *before* the PR merges, `markdown-lint` becomes required while `main` has no such job, and **every** open PR (including this one, if run pre-merge on a stale head) is blocked with an unreportable context. | Suggested fix: Add an explicit ordered rollout step to the spec: (1) merge the PR to `main`; (2) maintainer runs `bash scripts/apply-repo-config.sh`; (3) verify with `gh api repos/SMK1085/paigasus-helikon/rulesets/...`; and a rollback step (remove the context from the JSON and re-apply) if the gate turns out to be wrong. Make Verification #5 assert *enforcement*, not *listing*.

- **The self-test script is not implementable as specified — assertion 2 has no observable signal** - The spec requires asserting that `docs/book/src/SUMMARY.md` **is** in the linted set and a `docs/superpowers/` path **is not**, and then explicitly rejects a file-count assertion ("Deliberately assertions about set membership rather than a file **count**"). markdownlint-cli2 has no `--list-files`; on a clean run it prints only `Linting: N file(s)` and `Summary: 0 issue(s)`. The one machine-readable signal it emits — the count — is the one the spec forbids, and per-file lines appear only for files that *have* findings. | Why it matters: This is a named deliverable with its own verification criterion (Verification #2: "exits non-zero under each of its two mutations"). An implementer will either invent an undesigned mechanism, silently downgrade to the count the spec rejected, or write a script that passes vacuously. | Suggested fix: Prescribe the mechanism. A deterministic, implementable form: the script writes two throwaway probe files containing a known violation (e.g. `docs/book/src/__mdlint_probe.md` and `docs/superpowers/__mdlint_probe.md`), runs the real config, asserts the first path appears in the output and the second does not, then removes both in a `trap … EXIT`. Same trick makes assertion 1 concrete. Specify `set -euo pipefail` and a positive-marker assertion (grep for the expected `MD0xx` string), not absence-of-findings — per the repo's own lesson in `.github/workflows/audit.yml`'s `scheduled-audit` commentary.

- **The self-test's invocation would run a *different* markdownlint version than the gate, and adds a network dependency to a required check** - The proposed job has exactly two steps (checkout + `DavidAnson/markdownlint-cli2-action`), no `actions/setup-node`. `scripts/check-markdownlint-config.sh` is said to "run as a step in the `markdown-lint` job", and Verification #2 invokes it as `bash scripts/check-markdownlint-config.sh`. If it shells out to `npx markdownlint-cli2` (the only invocation the spec ever names, in the Verification section and CONTRIBUTING), npm resolves **latest**, not the action's bundled 0.23.2. | Why it matters: (a) The self-test would then certify a config against a linter the gate does not use — the guard is decoupled from the thing it guards. (b) It puts an unpinned npm registry fetch inside a *required* status check, which is a genuine flake source; the spec's entire "no signal-first phase needed" argument rests on the job being deterministic. (c) It contradicts the spec's own supply-chain reasoning ("`npx markdownlint-cli2@0.23.2` … has no checksum at all"). | Suggested fix: Decide and state one of: (a) drop the action, add a pinned `actions/setup-node` + `npm ci` against a committed `package.json`/`package-lock.json` with `markdownlint-cli2@0.23.2` (checksummed by the lockfile, one version for both the gate and the self-test, and identical to what contributors run); or (b) keep the action and have the self-test reuse the action's own resolved binary path, asserting the version explicitly (`markdownlint-cli2 --version` == `0.23.2`) so a bundled-version drift fails loudly. Either way the script must assert the version it is testing.

- **The proposed `globs` break the "bare `npx markdownlint-cli2` locally lints exactly what CI lints" invariant on this repo's own machines** - `.gitignore` line 12 ignores `.claude/worktrees/`, and `/private/tmp/.../wt-sma-581/tests/workspace-lints/tests/tracing_target_docs.rs:143-145` documents why it matters: "`.claude/worktrees/` can hold full checkouts of other branches, and scanning those would make this test's verdict depend on which unrelated worktrees a developer happens to have." markdownlint-cli2 does not honour `.gitignore` unless `"gitignore": true` is set, and it clearly traverses dot-directories here (the measured 51 includes `.github/PULL_REQUEST_TEMPLATE.md`). The negations are root-anchored, so `!docs/superpowers/**` does **not** exclude `.claude/worktrees/&lt;branch&gt;/docs/superpowers/**`. | Why it matters: The config file *is* the design's single source of truth, and this is a defect in it. On the maintainer's machine a bare local run would lint every `.md` in every checked-out worktree, including the 274 bare fences the spec deliberately excludes — the documented local gate is unusable, so contributors will stop running it and the "one source of truth" property evaporates. `target/package/` (produced by `cargo package` / release-plz verification) likewise contains full crate copies including `README.md`, so `!target/**` is load-bearing, not the "belt-and-braces" the spec calls it. | Suggested fix: Add `"gitignore": true` to `.markdownlint-cli2.jsonc` (which subsumes `target/`, `node_modules/`, `.claude/worktrees/`, and any future ignore), **and** keep the explicit negations as unanchored patterns (`!**/target/**`, `!**/node_modules/**`, `!**/.claude/**`) so the exclusion survives someone turning `gitignore` off. Add a self-test assertion that a path under `.claude/worktrees/` is not in the linted set.

### MAJOR

- **The prescribed `MD036` fix introduces four new `MD001` violations in the same file** - The spec says `4× MD036` in `docs/runbooks/forkd-live-validation.md` become `####`. The nearest preceding heading is `## Troubleshooting` at line 362; the four bold pseudo-headings are at lines 383, 388, 393, 398, with no `###` between them. `##` → `####` is a two-level skip, which is exactly what `MD001` (heading-increment, enabled by `"default": true`) forbids. | Why it matters: The fix set as written cannot satisfy Verification #1 ("exits 0 with `0 issues`"). Worse, an implementer who hits four fresh `MD001` errors is one bad decision away from adding `"MD001": false` repo-wide — the outcome the spec's "No rule is disabled repo-wide in order to dodge a finding" paragraph exists to prevent, and it would silently un-fix the `crates/paigasus-helikon-tools/README.md:9` finding in the same PR. | Suggested fix: Change the prescription to `###`, and add an explicit instruction: "if a heading-level fix produces a new `MD001`, adjust the level — never disable `MD001`."

- **Dependabot's patch+minor group *can* change the bundled linter and enable new rules unattended** - The spec's safety argument is: "Dependabot's `github-actions` group is patch+minor only, so a v24→v25 bump never arrives unattended … Bumping it is a human act." That only covers *majors*. `markdownlint-cli2-action` ships new bundled `markdownlint-cli2` versions in **minor** releases, and markdownlint itself introduces new rules in minors (MD056/MD058/MD059/MD060 all arrived this way). Under `"default": true`, a new rule is live the moment it lands. | Why it matters: A routine grouped `chore(deps)` PR can redden a **required** gate for reasons nobody chose, on `main` as well as on the PR. And per the repo's own operational constraint, the `branch-names` ruleset blocks humans pushing to `dependabot/**`, so fixing a Dependabot PR in place is impossible — it needs a `feature/**` takeover PR. The grouped PR bundles every other action bump with it, so one new markdown rule blocks the whole group. | Suggested fix: State this failure mode explicitly and pick a mitigation: either (a) add `markdownlint-cli2-action` to a Dependabot `ignore` for minor updates and hand-bump it alongside `PROTOC_VERSION`/`NIGHTLY_TOOLCHAIN` in CLAUDE.md's list of untracked pins; or (b) decouple by pinning `markdownlint-cli2@0.23.2` in a lockfile (see the third BLOCKER) so an action bump cannot move the ruleset; or (c) accept it and document the runbook ("a red `markdown-lint` on a Dependabot PR usually means a new rule — triage before fixing content"), mirroring the existing "a red audit/deny gate is usually NOT your PR" pattern.

- **Dropping `.markdownlint-cli2.jsonc` at the repo root changes CodeRabbit's behaviour, and the spec's stated alignment mechanism is the wrong one** - `/private/tmp/.../wt-sma-581/.coderabbit.yaml` configures only the Linear integration; there is no `tools.markdownlint` block. CodeRabbit auto-discovers repo-root markdownlint config. The spec justifies its version choice as "byte-identical to the version CodeRabbit runs, satisfying the ticket's alignment requirement" — but *version* parity is not what makes CodeRabbit and CI agree; *config* parity is, and that is a side effect the spec never analyses. | Why it matters: Two concrete consequences go unexamined. (1) Good: CodeRabbit will now honour `MD013: false` and `MD060: compact`, so its advisory comments stop diverging from CI. (2) Risky: the spec's own central mechanism is that command-line globs **union** with the config's `globs`. If CodeRabbit passes changed-file paths, the union with `**/*.md` means CodeRabbit may start reporting markdown findings on all 51 files on every PR, not just the diff — turning a helpful reviewer into noise, in the same PR that adds the gate. Separately, "byte-identical to what CodeRabbit runs" is a claim about a third party's internal pin that this repo cannot observe or hold stable; it will silently become false. | Suggested fix: Do not justify the pin by CodeRabbit parity — justify it on its own merits and state that config discovery, not version matching, is what aligns the two. Add a post-merge check to the plan: confirm on the first PR after merge that CodeRabbit's markdown comments are still diff-scoped, and add an explicit `tools.markdownlint` block to `.coderabbit.yaml` if not.

- **The `--fix` diff is not "entirely whitespace and table-pipe padding"** - The auto-fixable 98 necessarily includes the single `MD034` (bare-URL) and `MD038` (spaces-inside-code-span) findings — that is how 77+10+4+4+3 reaches 98 and how the residual is exactly 12. `MD034`'s fix rewrites `https://x` to `&lt;https://x&gt;`; `MD038`'s fix deletes characters inside a code span. | Why it matters: The spec uses "entirely whitespace" to argue the diff is reviewable-at-a-glance and to bound the blast radius on crates.io/docs.rs pages. Both fixes are content edits, and if either lands in `crates/paigasus-helikon/README.md` it lands in rendered rustdoc (the facade README is `include_str!`'d into `crates/paigasus-helikon/src/lib.rs`). | Suggested fix: Correct the characterisation, and add the `MD034`/`MD038` files to the per-finding disposition table so a human eyeballs them rather than rubber-stamping "whitespace".

- **The affected-crate list in Consequences is wrong — at least five crates bump, not four** - Consequences names `providers-litellm`, `runtime-actix`, `runtime-axum`, `runtime-temporal`. But the fix set explicitly edits `crates/paigasus-helikon-tools/README.md:9` (the `MD001` h1→h3 fix), and the `--fix` set is stated as 12 files without a per-file listing. | Why it matters: The spec is otherwise scrupulous about release-plz consequences, and CLAUDE.md's warning is that a README touch cascades. Getting the list wrong means the release PR contains a bump the reviewer was told not to expect — precisely the surprise this section exists to prevent. | Suggested fix: Enumerate all 12 `--fix`-touched files by path in the spec (the author already has the diff), derive the crate list from that, and state affirmatively that this is the **pure-auto** release path — release-plz performs the bumps itself, so `dependencies_update` cascades correctly and **no** manual `core`/facade bump is needed (CLAUDE.md's same-PR-manual-bump caveat does *not* apply here). Also consider the mitigation the spec never names: split into two PRs — a `docs(readme)`/`chore` PR carrying the whitespace, and a `ci(workflows)` PR carrying the gate — so the release churn is isolated from the plumbing.

- **The `SUMMARY.md` fallback silently weakens the gate across 20 files, and a better third option exists** - The fallback is "a scoped `docs/book/src/.markdownlint.jsonc` containing `{ "MD025": false }`". That disables single-title checking for *every* book page, not the one file where the rule is genuinely wrong. | Why it matters: The spec's own stated principle is file-scoped disables, "never a repo-wide `false`" — the fallback violates the spirit of that on the repo's most-read published surface. Neither branch is verified before implementation, so the design is being handed off with a coin-flip in it. | Suggested fix: There is a third option that dodges the parse question entirely: `markdownlint-disable-file` is position-independent, so put the comment **after** the `# Summary` H1 (line 2 of `docs/book/src/SUMMARY.md`) or at end-of-file, rather than leading. mdBook 0.4.43's summary parser is very likely to tolerate a leading comment (its `parse_title` skips HTML events), but placing it after the title makes the question moot. Verify both placements against `mdbook build docs/book` **during design**, not during implementation, and delete the directory-wide fallback.

- **"PR #218 must rebase" — the mechanism and the remedy are both wrong** - `.github/rulesets/main-protection-checks.json` sets `"strict_required_status_checks_policy": false`, so GitHub does *not* require the PR to be up to date with base. The reason the context never reports is not that "its head predates the workflow" but that no `pull_request` event fires when `main` moves. `.github/workflows/docs.yml` uses a bare `pull_request:` trigger, so the default types (`opened`, `synchronize`, `reopened`) apply. | Why it matters: The spec uses this cost estimate ("it need only rebase") as a load-bearing input to the required-immediately decision, and a rebase on a repo with `required_linear_history` plus signed commits is not free. | Suggested fix: Correct to: any event that re-triggers the workflow suffices — an empty commit, or simply close-and-reopen the PR (the `reopened` type is active). Note the corollary that this *also* means the new job runs on its own PR, so the chicken-and-egg is not a problem.

- **The determinism argument is about the linter, not about the job — and the job as designed is not deterministic** - "markdownlint is deterministic. There is no flake rate to measure" is true of the tool in isolation. The job as specified adds (a) an npm/npx fetch inside the self-test step and (b) an action whose minor bumps change the ruleset. Both are non-determinism sources introduced *by this design*, not inherent to markdown linting. | Why it matters: The whole "no signal-first phase" case rests on this claim, and the required-immediately decision has repo-wide blast radius. | Suggested fix: Don't stage the gate — remove the non-determinism instead. Eliminate the network fetch (lockfile or reuse the action's binary), pin the linter independently of the action, and then the determinism argument is actually sound and the immediate-required decision is defensible. Add `timeout-minutes:` to the job so a hung npm fetch fails fast rather than sitting for six hours on a required context.

- **No `.gitattributes` rule for `*.md`, while CONTRIBUTING will instruct contributors to run `--fix`** - `/private/tmp/.../wt-sma-581/.gitattributes` pins only `crates/paigasus-helikon-providers-anthropic/tests/fixtures/*.txt` to `text eol=lf`. | Why it matters: CI is ubuntu-only so the gate itself is stable, but the spec adds `npx markdownlint-cli2` to CONTRIBUTING's local-gates list, and the repo's CI matrix explicitly includes Windows contributors. A Windows `--fix` rewrites files with the platform newline, producing whole-file diffs and a CI/local mismatch that looks like a linter bug. This is the same failure the repo already learned once, documented in CLAUDE.md's "Fixture line endings" section. | Suggested fix: Add `*.md text eol=lf` to `.gitattributes` in the same PR, and mention it in the spec's file list.

### MINOR

- **The `MD028` disposition is ambiguous enough that two engineers build different things** - `docs/runbooks/agentcore-image-check.md` lines 3-23 and 25-40 are two deliberately separate blockquotes (a 2026-07-06 local measurement and a 2026-08-07 CI measurement). The spec's disposition is one word: "fix — blank line inside a blockquote." Merging them into one blockquote and inserting an `&lt;!-- --&gt;` separator render differently and mean different things. | Suggested fix: Prescribe the separator (`&lt;!-- --&gt;` between the two blockquotes), which preserves the authorial intent.

- **The enumerated Markdown surfaces are incomplete, and two gated files are effectively vendored** - The Problem section lists `docs/book/`, `crates/*/README.md`, and four root files. The measured 51 also includes `/private/tmp/.../wt-sma-581/docker/forkd/README.md` (never mentioned anywhere), `CODE_OF_CONDUCT.md` (a verbatim copy of Contributor Covenant 2.1), and `CLAUDE.md`. | Suggested fix: List the full 51 by directory in the spec. Consider excluding `CODE_OF_CONDUCT.md` for the same reason `crates/*/CHANGELOG.md` is excluded — it is upstream text nobody here authored, and `--fix` diverges it from the canonical document.

- **Glob-union asymmetry between CI and local** - The action's default `globs` input is `*.{md,markdown}`; the config supplies `**/*.md`. The union therefore includes root-level `*.markdown` files in CI that a bare local `npx markdownlint-cli2` would not lint. No such file exists today, so this is theoretical — but the spec asserts exact parity as a design property. | Suggested fix: Either add `**/*.markdown` to the config globs, or pass `globs: ""`/an explicit value in the workflow so the union is a no-op, and say which.

- **`MD040` now interacts with the facade README's doctests** - `crates/paigasus-helikon/src/lib.rs` does `#![doc = include_str!("../README.md")]`, so every fenced block in `crates/paigasus-helikon/README.md` is compiled by `cargo test`. Enforcing "every fence needs a language" pushes authors toward ```` ```rust ````, which turns prose snippets into doctests. | Suggested fix: One sentence in the spec (and in CONTRIBUTING): in the facade README, use ```` ```text ```` or ```` ```ignore ````, never a bare ```` ```rust ```` for a snippet that needs network or keys.

- **"No mdBook or crate-README *content* changes" contradicts the fix set** - The closing section says this, while the fix set explicitly changes a heading level in `crates/paigasus-helikon-tools/README.md` and `--fix` touches four more crate READMEs. | Suggested fix: Reword to "no *documentation content* is rewritten; the only README edits are the mechanical fixes enumerated above," and keep the conscious-skip statement about the mdBook prose pages.

- **CLAUDE.md's CI section does not currently describe `docs.yml` at all** - It names `book-build` in the required-contexts list but never says which workflow hosts it, and enumerates `ci.yml` (eight jobs), `integration.yml`, `pr-title.yml`, `msrv.yml`, `audit`/`deny`/`sbom`. | Suggested fix: The doc update is not "append a line to the CI section" — it needs to introduce `docs.yml` as a required-check-bearing workflow first, otherwise `markdown-lint` appears in the roster with no home.

- **One real argument for `ci.yml` over `docs.yml` that the spec does not weigh** - `docs.yml`'s `book-deploy` only `needs: book-build`, so a `markdown-lint` failure on a push to `main` reddens the `docs` **workflow run** while the Pages deploy still succeeds. That makes "the docs workflow is red" ambiguous between "the site failed to publish" and "a heading was wrong" — the same signal-legibility problem CLAUDE.md's signal-only-vs-required reasoning is built to avoid. | Suggested fix: Either move the job to `ci.yml` (where every other required non-book gate lives and where the run conclusion has one meaning), or state this consequence explicitly and accept it. The spec's current justification ("docs.yml is the Markdown/docs workflow and already carries a required check") is fine as far as it goes but does not address this.

### QUESTIONS

- What does `MD060: { "style": "any" }` mean if it yields 66 findings rather than 0? If `any` is the rule's *default*, then `"default": true` alone already carries 66 MD060 findings, and the Decisions table's framing of `compact` as a choice among four equals needs restating. This matters because it determines what happens if the `style` key is ever dropped.
- Does markdownlint-cli2 0.23.2 error on an unknown *rule name* key (e.g. a typo'd `"MD6O": false`), or silently ignore it? The self-test's failure mode #1 covers an invalid option *value* only. If unknown names are also silent, the self-test has a third hole and needs a third assertion.
- Would the `docs/book/src/.markdownlint.jsonc` fallback be copied into the published Pages output by mdBook's asset copier? (Probably not — mdBook skips dotfiles — but it should be checked before the fallback is offered, since it would publish repo tooling config to a public site.)
- Who runs `scripts/apply-repo-config.sh`, and at what point relative to merging PR #218 and this PR? (See BLOCKER 1.)
- How would anyone detect that CodeRabbit's bundled markdownlint version has drifted away from 0.23.2, given the spec makes that parity a stated requirement?
- Why not `"gitignore": true` in the config? It is the single-line fix for three of the four exclusions and for the worktree problem; if it was rejected, the reason belongs in the spec.
- Is `--fix` expected to touch `docs/book/src/concepts/observability-evaluation.md`? Verification #4 correctly guards `tests/workspace-lints/tests/tracing_target_docs.rs`, and I confirmed that test's parser (`.trim()` on cells, separator detection via `all(|c| c == '-' || c == ':')`) survives any `MD060` normalisation — but `MD058` (blanks-around-tables) could insert a blank line immediately after `&lt;!-- tracing-components:start --&gt;`. Worth stating that the guard was reasoned about, not just run.

## What's good

- **Denylist over allowlist, with a concrete failure citation.** Tying the choice to the observed drift ("13 stub pages through all of Stage 1") is exactly the right justification shape, and it means `docker/forkd/README.md` — a surface the spec's own prose forgot — is gated anyway. Keep this.
- **The self-test *concept*.** Naming two specific silent-success modes, both hit during design, and preferring set-membership assertions over a brittle count is precisely the house style of `scripts/check-advisory-ignore-sync.sh` and `.github/actions/setup-protoc/selftest.sh`. The implementation needs rework; the instinct does not.
- **Correcting the ticket with measured numbers, and arithmetic that actually reconciles.** 928 raw − 818 MD013 = 110 gated; 98 auto-fixable + 12 hand-fixes = 110; and the 12 in the disposition table sum correctly. That internal consistency is what made the `MD034`/`MD038` mischaracterisation findable at all. Also correct and non-obvious: excluding `crates/*/CHANGELOG.md` so a bot-authored release PR cannot go red for Markdown no human wrote.</result>
<usage><subagent_tokens>107564</subagent_tokens><tool_uses>22</tool_uses><duration_ms>523185</duration_ms></usage>
</task-notification>