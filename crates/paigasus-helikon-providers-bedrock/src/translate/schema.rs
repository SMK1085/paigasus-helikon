//! JSON schema translation rulesets for the Bedrock Converse API.

/// A ruleset for translating JSON schemas to Bedrock-compatible tool schemas.
#[derive(Debug, Default, Clone)]
pub struct Ruleset {
    _priv: (),
}
